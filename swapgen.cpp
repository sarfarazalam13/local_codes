#include<iostream>
using namespace std;
int swap(int x, int y)
{
    int temp;
    temp=;
    a = b;
    b = temp;
    return (a, b);
}
int main()
{
    int a, b,s;
    cin >> a >> b;
    swap(a, b);
    cout << a;
}